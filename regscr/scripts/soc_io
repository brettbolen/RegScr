# read/write from mcbsp
BASE=0x1d020
for i in $*
do
	p1=`dirname $i`
	p2=`basename $i`
	#echo p1 is $p1
	#echo p2 is $p2
	if [ .$p1 == .. ]
	then
		echo -n "Reg Read  $p2 << "
		devmem ${BASE}${p2}
	else
		devmem ${BASE}${p1} 32 0x${p2}
		echo -n "Reg write $p1 >> "
		devmem ${BASE}${p1}
	fi
done

